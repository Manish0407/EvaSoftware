<div class="container-fluid" style="margin-bottom: 309px; margin-top: inherit;">
  <div class="row">
    <h3 class="text-left col-10">Territory master</h3>
    <span class="col-2 text-end"><%= link_to 'Add territory master', new_territory_master_path, class: 'btn btn-primary text-decoration-none text-white' %></span>
  </div>
  <div class="container card mt-4" style="border: none;">
    <div class="card-body">
      <div class="table-responsive" style="overflow: hidden;">
        <table class="table table-hover">
          <thead>
            <tr>
              <th>Territory name</th>
              <th>State name</th>
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            <% @territory_masters.each do |territory_master| %>
              <tr>
                <td><%= territory_master.territory_name %></td>
                <td><%= territory_master.territory_description %></td>
                <td class="position-relative">
                  <div class="dropdown dropup">
                    <button class="btn btn-sm dropdown-toggle" type="button" id="actionMenu<%= territory_master.id %>" data-bs-toggle="dropdown" aria-expanded="false" style="background: aliceblue; border-radius: 50%;">
                      &#x22EE;
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end">
                      <li><%= link_to 'Edit', edit_territory_master_path(territory_master), class: 'dropdown-item text-decoration-none' %></li>
                      <li><%= link_to 'Delete', territory_master_path(territory_master), method: :delete, data: { confirm: 'Are you sure?' }, class: 'dropdown-item text-decoration-none' %></li>
                    </ul>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div> 
</div>
